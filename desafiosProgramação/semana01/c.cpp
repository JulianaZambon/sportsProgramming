#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, q;  /* tamanho do texto e qntd de consultas */
    cin >> n >> q;
    string s; /* texto s com n caracteres latinos */
    cin >> s;

    /* seguem q consultas cada uma em uma linha */
    /* linha da consulta = t (tipo da consulta) e x */
    /* se t == 1 o inteiro X identifica quantas vezes a operação deve ser repetida */
    /* se t == 2 qual o caractere a ser impresso do texto */

    /* sempre vai haver pelo menos uma consulta do tipo 2 */

    return 0;
}
